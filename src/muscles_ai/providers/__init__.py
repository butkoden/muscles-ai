"""Optional in-process model provider adapters.

Modules in this package must keep heavyweight SDK imports lazy. The base
``muscles-ai`` installation remains usable with fake/noop/python providers.
"""
